const entrada = require('readline-sync');

const placa = entrada.question("Numero da placa: ");
const velocidade = entrada.question("km/h: ");

if (velocidade >= 80) {
    console.log("velocidade acima de 80km/h MULTADO! ");
} else if (velocidade >= 80 && media < 0); {
    console.log("velocidade abaixo de 80km/h Continue sua viagem ");
}